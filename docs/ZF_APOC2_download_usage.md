# Download and Usage Instructions

This page is a draft for the future public release. Replace placeholder S3
values after the dataset is uploaded.

## Dataset Root

AWS S3 dataset root:

```text
root
```

Planned structure:

```text
ZF_APOC2/
├── model/
│   ├── bodipy/
│   ├── brightfield/
│   └── oil_red_o/
└── drug_screen/
```

## AWS S3 Access

After upload to AWS S3, users should be able to browse or download the dataset from a public bucket.

S3 URI:

```text
root
```

Example commands:

```bash
aws
```
